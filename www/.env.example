# Copy this file to .env.local and fill in real values.
# .env.local is gitignored — never commit it.

# api's base URL — where this app's authClient points. Defaults to
# api's local dev server; set to the real deployed api URL in production.
VITE_API_URL=http://localhost:3000
