# Copy this file to .dev.vars and fill in real values.
# .dev.vars is gitignored — never commit it.

# infra's base URL — where this app's authClient points. Defaults to
# infra's local dev server; set to the real deployed infra URL in production.
VITE_INFRA_URL=http://localhost:3000
